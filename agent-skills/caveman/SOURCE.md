# Source

Originally from [mattpocock/skills](https://github.com/mattpocock/skills), MIT licensed,
at `skills/productivity/caveman/SKILL.md`.

Installed here on 2026-06-04 via the `skills` CLI. Upstream removed the skill later, so this
copy is now maintained by envi instead. It is not kept in sync with the upstream repository
(see `ENVI_AGENT_SKILL_SOURCES` for the skills that are).
