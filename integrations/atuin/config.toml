# Envi default atuin config.
# Installed on first run if ~/.config/atuin/config.toml does not exist.
# Edit freely afterwards; envi will not overwrite an existing file.

style = "compact"
inline_height = 10
show_numeric_shortcuts = false

[ui]
columns = ["time", "command"]
